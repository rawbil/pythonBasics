# Tuples
# Containers like lists, but are immutable
# Once created, elements cannot be changed or added.
# A comma separated instance of values defines a tuple

tuple1 = 1, 2, 3
print(tuple1)  # (1, 2,3 )
